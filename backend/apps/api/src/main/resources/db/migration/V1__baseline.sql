-- Phase A baseline: Flyway is configured and runnable.
-- No business tables are created here (Phase3 §3 schema starts with Module 1 — Auth).
-- Subsequent migrations must follow the frozen architecture; never invent tables.

SELECT 1;
